@using CrudApp.ViewModels
@model IndexViewModel

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center my-4">All Things</h1>

<div class="text-center mb-4">
    <a asp-action="Create" class="btn btn-success">Create New</a>
</div>

<form asp-action="Index" method="get" class="mb-4">
    <div class="input-group">
        <input type="text" name="searchString" value="@ViewData["CurrentSearch"]" class="form-control" placeholder="Search for things..." />
        <button type="submit" class="btn btn-primary">Search</button>
    </div>
</form>

<h2>Things from SqlSDatabase:</h2>
<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Things.FirstOrDefault().Title)</th>
            <th>@Html.DisplayNameFor(model => model.Things.FirstOrDefault().Description)</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Things)
        {
            <tr>
                <td>@Html.DisplayFor(modelitem => item.Title)</td>
                <td>@Html.DisplayFor(modelitem => item.Description)</td>
                <td class="text-center">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<h2>Things from MongoDb:</h2>
<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>Title</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.MongoThings)
        {
            <tr>@item.Title</tr>
            <tr>@item.Description</tr>
        }
    </tbody>
</table>
